import './styles/main.scss';
import App from './App.js';

/**
 * Progressive enhancement: todo el contenido (h1, textos, enlaces legales)
 * vive en el HTML. Sin WebGL la web sigue funcionando sin el corazón.
 */
new App().start();
